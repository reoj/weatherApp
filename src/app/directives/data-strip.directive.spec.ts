import { DataStripDirective } from './data-strip.directive';

describe('DataStripDirective', () => {
  it('should create an instance', () => {
    const directive = new DataStripDirective();
    expect(directive).toBeTruthy();
  });
});
